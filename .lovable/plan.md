## ✅ Correção aplicada — Limite de mercados expandido

Linha 114 de `comparar-precos-lista/index.ts` alterada de `.slice(0, 3)` para `.slice(0, 15)`.

Todos os mercados dentro do raio do usuário agora participam da comparação de preços.
